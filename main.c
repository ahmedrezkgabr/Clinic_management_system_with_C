#include<stdio.h>#include"STD_Types.h" /*struct that contain patient information*/struct node{	u8 name[100];		/*patient name */	u8 age;				/*patient age */	u8 gender;			/*patient gender */	u16 ID;				/*patient ID */}; /*global variables*/u8 i = 0;			/*variable for index of the array of structs*/u8 slots[5] = {0};	/*array of the slots						*/u8 choice = 0; /*variable for the choosing					*/ /*array of structs*/struct node patients[100] = {0}; /*Admin mode functions*/void add();void edit();void reserve();void cancle(); /*User mode functions*/void view();void reservation(); /*entry point*/int main(void){ 	u16 password;		/*Admin Password -> 1234 */	u8 counter = 0;		/*for 3 trails */	u8 flag;			/*indecation for is password enterd true or not*/	while(1)	{		printf("\n");		printf("----------------------------------------------\n");				printf("WELLCOME\n");		printf("1: for admin\n");		printf("2: for user\n");				printf("CHOOSE MODE\n");		scanf("%d",&choice);				switch(choice)		{			case 1:			/*Admin mode*/				printf("\n");				printf("WELCOME AT ADMIN MODE (:\n");								/*log in process*/				for(;;)				{					printf("\n");					printf("Please enter the password\n");					scanf("%d",&password);					if(password == 1234)					{						printf("\n");						printf("LOG IN (:\n");						flag = 1;						break;					}					if(counter == 2)					{						printf("\n");						printf("YOU CAN NOT LOG IN ):\n");						flag = 0;						break;					}					printf("WRONG PASSWORD\n");					counter++;				}												/*the actual actions of the admin*/				if(flag == 1)				{					counter = 0;		/*making counter equal 0 for the next loop inside while(1) */					choice = 0;			/*making choice equal 0 to use it again in the next choosing*/									printf("\n");					printf("1: Add New Patient Record\n");					printf("2: Edit Patient Record\n");					printf("3: Reserve A Slot With The Doctor\n");					printf("4: Cancle Reservation\n");										printf("CHOOSE MODE\n");					scanf("%d",&choice);										switch(choice)					{						case 1:			/*Admin mode -> adding a new patient*/							printf("\n");							printf("1: Add New Patient Record\n");							add();							break;													case 2:			/*Admin mode -> editng a patient information*/							printf("\n");							printf("2: Edit Patient Record\n");							edit();							break;													case 3:			/*Admin mode -> reserving a slot for a patient*/							printf("\n
